const btn = document.getElementById('btn');
const mensaje = document.getElementById('mensaje');

btn.addEventListener('click', () => {
    mensaje.textContent = '¡Genial! Tu página está funcionando correctamente 🚀';
});
